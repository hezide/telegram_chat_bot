services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    environment:
      - ALLOWED_ORIGIN=http://localhost
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
